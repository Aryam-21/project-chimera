[project]
name = "project-chimera"
version = "0.1.0"
description = "Spec-driven autonomous influencer infrastructure"
authors = [
    { name = "Aryam Tesfay", email = "your-email@example.com" }
]
requires-python = ">=3.10"

dependencies = [
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
